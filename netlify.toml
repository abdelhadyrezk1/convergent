# Force Netlify to build only the static frontend with Vite
[build]
  command = "npx vite build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA routing so deep links work
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
